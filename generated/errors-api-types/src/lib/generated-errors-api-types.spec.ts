import { generatedErrorsApiTypes } from './generated-errors-api-types';

describe('generatedErrorsApiTypes', () => {
  it('should work', () => {
    expect(generatedErrorsApiTypes()).toEqual('generated-errors-api-types');
  });
});
